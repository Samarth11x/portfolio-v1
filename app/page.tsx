/**
 * @file app/page.tsx
 * @description Homepage — Sprint 0 placeholder.
 *
 * This file will be replaced in Sprint 1 with the full hero section
 * and page composition. For now it renders nothing visible so the
 * build passes TypeScript and lint checks cleanly.
 */

export default function HomePage() {
  return (
    <main>
      {/* Sprint 1: Hero, About, Projects, Skills, and Contact sections */}
    </main>
  );
}
