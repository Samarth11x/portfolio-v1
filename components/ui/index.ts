/**
 * @file components/ui/index.ts
 * @description Barrel export for all primitive UI components.
 *
 * Primitive components (Button, Badge, Card, etc.) are built here.
 * They should be fully unstyled-opinionated at the data layer and
 * styled purely through className props and the `cn` utility.
 *
 * Import pattern: `import { Button } from '@/components/ui'`
 */

// Components will be added in Sprint 1+
// export { Button } from './Button';
// export { Badge } from './Badge';
// export { Card, CardHeader, CardBody, CardFooter } from './Card';
